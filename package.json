{
  "name": "asistencia-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=asistencia-web",
    "dev:api": "npm run dev --workspace=asistencia-api",
    "build": "npm run build --workspaces"
  }
}
